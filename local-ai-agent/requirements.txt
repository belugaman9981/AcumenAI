# Core — all local, no API services needed
requests>=2.31.0
beautifulsoup4>=4.12.0
rich>=13.7.0
ddgs>=0.1.0
duckduckgo-search>=6.1.0   # fallback if ddgs not available
lxml>=5.1.0

# Web server (bridges the website to the local brain)
flask>=3.0.0
flask-cors>=4.0.0

# PDF ingestion (WSJ articles, books, docs)
pypdf>=3.0.0
pdfplumber>=0.10.0

# Screenshot analyzer
Pillow>=10.0.0
mss>=9.0.0
pytesseract>=0.3.10

# Voice mode (optional)
pyttsx3>=2.90
SpeechRecognition>=3.10.0
# pyaudio>=0.2.14  # uncomment for mic input
